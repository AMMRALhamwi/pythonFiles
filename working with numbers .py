# if you want to have the absoulod valu use abs
num = -55
print (abs(num))
# to put a num into power 
print (pow(3 , 3))
# to return the larger number
print (max (5 , 7))
# to return the smaller number
print (min ( 6 , 8))
# to round a num into its closest int
print (round(4.3))
 #to import some other fuctions 
from math import *
  # now only we can things like this
  # to get the lowest number
print (floor (7.7))
 # to get the highest number 
print (ceil (7.7))
# to get the squer root 
print (sqrt (9))
